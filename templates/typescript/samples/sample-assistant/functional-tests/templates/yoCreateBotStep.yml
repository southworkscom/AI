steps:
- powershell: |
   if ("$(BotName)" -ne "")
   {
      yo bot-virtualassistant -n $(BotName) -d "" -l "en-us" -p "$(System.DefaultWorkingDirectory)/templates/typescript/samples/functionalTests" --noPrompt
   }
  displayName: 'Prepare: Create bots using the generator'
